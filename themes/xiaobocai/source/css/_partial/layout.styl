// 全局变量
:root {
  --max-content-width: 900px;
  --deep-blue: #192231;
  --link-hover: #c80000;
}

// 去除body自带的margin
body {
  margin: 0;
}

// 主体内容
.main {
  max-width: var(--max-content-width);
  margin: 0 auto;
}
